TODO

* Removed call to modify_close_cmd in Parser#handle_command, and removed modify_close_cmd test in parser_test.rb
* Load command classes on Parser.new instead of Papyrus.new
* Add guarded lexicon reader in Papyrus
* Fix Parser#modify_active_cmd to accept command name and args, not full command
* Parser#populate_template: only add command from handle_command if not nil (as handle_command_close will return nil)
* Change BlockCommand#initialize and #add to return NotImplementedError
* Add BlockCommand#active_block to raise NotImplementedError
* Implement active_block for BlockCommand subclasses
* Add BlockCommand#add, remove BlockCommand subclass #add's
* ContextItem#get should return key if key starts with a number
* Add Papyrus.cache_templates writer and .cache_templates? reader, modified Parser to only cache the template if this option is true
* Rename 'recording' methods in TokenList to 'stashing', add #stash_curr to append the current token to the stash

* Better if statement parsing -- copy from Liquid. The if command either accepts one arg (value), or three args (value1, op, value2).

* Replace Loop with For
* Do we need Variable anymore? Include?
* Do we need Comment?
* Include needs to be fixed
* Update documentation!!

FUTURE

* Should we add attr_accessors for all instance variables for easier access during
  testing?